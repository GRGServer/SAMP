enum enum_permission
{
	PERMISSION_ADMIN,
	PERMISSION_MAPPER,
	PERMISSION_MODERATOR,
	PERMISSION_NPCADMIN,
	PERMISSION_SERVERADMIN,
	PERMISSION_TELEPORT
}

enum enum_pickup
{
	PICKUP_ID,
	PICKUP_TYPE
}

enum enum_pickupLoader
{
	PICKUPLOADER_TYPE[256],
	Float:PICKUPLOADER_POSX,
	Float:PICKUPLOADER_POSY,
	Float:PICKUPLOADER_POSZ
}

enum enum_vehicle
{
	VEHICLE_SAVEID,
	Float:VEHICLE_CURRENTFUEL,
	VEHICLE_PAINTJOB,
	VEHICLE_COLOR1,
	VEHICLE_COLOR2
}

enum enum_vehicleModel
{
	Float:VEHICLEMODEL_FUELUSAGE,
	Float:VEHICLEMODEL_MAXFUEL
}